import "tfplan3rdgen" 

allBDs = tfplan3rdgen.find_all_resources("aci_bridge_domain")

violatingBDs = tfplan3rdgen.filter_attribute_does_not_match_regex(allBDs,
                      "description", "^demo(.+)", true)                      

main = rule {
  length(violatingBDs["messages"]) is 0
}

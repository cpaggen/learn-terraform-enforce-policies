import "tfplan/v2" as tfplan
import "strings"
import "types"
import "tfplan3rdgen"

allBDs = find_all_resources("aci_bridge_domain")

violatingBDs = filter_attribute_does_not_match_regex(allBDs,
                      "description", "^demo(.+)", true)                      

main = rule {
  length(violatingBDs["messages"]) is 0
}

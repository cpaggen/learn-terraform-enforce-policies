import "tfplan3rdgen" as mycode

allBDs = mycode.find_all_resources("aci_bridge_domain")

violatingBDs = mycode.filter_attribute_does_not_match_regex(allBDs,
                      "description", "^demo(.+)", true)                      

main = rule {
  length(violatingBDs["messages"]) is 0
}
